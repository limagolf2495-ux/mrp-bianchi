[theme]
# ── MRP Bianchi · tema "Claro corporativo" ──────────────────────────────
base = "light"
primaryColor = "#7a1f2b"          # acento vino (botones primarios, foco)
backgroundColor = "#f1f3f6"       # fondo neutro frío
secondaryBackgroundColor = "#ffffff"  # superficies / sidebar / inputs
textColor = "#1f2733"
font = "sans serif"

[server]
runOnSave = true
